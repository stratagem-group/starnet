# Retired: Xenexus is an internal application with no automatic distribution.
name: retired-release
on:
  workflow_dispatch:
permissions: {}
jobs:
  retired:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Publication is disabled. A new owner-controlled design is required.'
