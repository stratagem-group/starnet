# Retired: internal Xenexus never mirrors or publishes upstream binaries.
name: retired-source-release-mirror
on:
  workflow_dispatch:
permissions: {}
jobs:
  retired:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Publication is disabled. A new owner-controlled design is required.'
