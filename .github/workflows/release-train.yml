# Retired: no upstream release destinations, signing identity or activation flags.
name: retired-release-train
on:
  workflow_dispatch:
permissions: {}
jobs:
  retired:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Publication is disabled. A new owner-controlled design is required.'
